{
  "$schema": "https://raw.githubusercontent.com/artalar/tabthrough/main/schema/guide-v1.json",
  "version": 1,
  "scope": { "kind": "workingTree", "base": "HEAD" },
  "summary": "$schema now points at the raw GitHub schema so editors and agents fetch a real file instead of tabthrough.dev. One URL, stamped by serialize, declared as $id, taught by the skill. Copies and fixtures follow.",
  "files": {
    ".cursor/skills/tabthrough/SKILL.md": { "significance": "skip", "rationale": "Byte-for-byte copy of res/skill/tabthrough/SKILL.md" },
    ".agents/skills/tabthrough/SKILL.md": { "significance": "skip", "rationale": "Byte-for-byte copy of res/skill/tabthrough/SKILL.md" },
    ".guide.json": { "significance": "skip", "rationale": "Legacy sidecar $schema only" },
    "README.md": { "significance": "skip", "rationale": "Example $schema follows the same URL" },
    "test/fixtures/guides/helper-consumer-merged.guide.json": { "significance": "skip", "rationale": "Fixture $schema follows the same URL" },
    "test/fixtures/guides/helper-consumer.dogfood.guide.json": { "significance": "skip", "rationale": "Fixture $schema follows the same URL" },
    "test/fixtures/guides/helper-consumer.guide.json": { "significance": "skip", "rationale": "Fixture $schema follows the same URL" },
    "test/fixtures/guides/valid.guide.json": { "significance": "skip", "rationale": "Fixture $schema follows the same URL" },
    "test/unit/published-schema.test.ts": { "significance": "skip", "rationale": "Assertion updated to the same URL" },
    "work-docs/architecture/guide-schema.md": { "significance": "skip", "rationale": "Docs drop the unhosted tabthrough.dev $id" },
    "work-docs/decisions/0003-rename-tabthrough.md": { "significance": "skip", "rationale": "ADR no longer plans a tabthrough.dev redirect" },
    "work-docs/progress/backlog.md": { "significance": "skip", "rationale": "P1-1 hosting leftover removed" },
    "work-docs/progress/iteration-log.md": { "significance": "skip", "rationale": "Historical note aligned to the resolvable URL" },
    ".tabthrough-guide.json": { "significance": "skip", "rationale": "The sidecar this walk is written into" }
  },
  "steps": [
    {
      "id": "stamp",
      "order": 10,
      "path": "src/guide/serialize.ts",
      "ranges": [{ "start": 4, "end": 4 }],
      "significance": "critical",
      "title": "Emitted guides stamp the raw URL",
      "rationale": "This is the $schema agents and editors try to fetch",
      "notes": "tabthrough.dev was never hosted. The raw GitHub path is the file that actually exists."
    },
    {
      "id": "schema-id",
      "order": 20,
      "path": "schema/guide-v1.json",
      "ranges": [{ "start": 3, "end": 3 }],
      "significance": "high",
      "title": "$id is that same URL",
      "rationale": "The document the fetch should return identifies itself with the fetch URL",
      "dependsOn": ["stamp"]
    },
    {
      "id": "skill",
      "order": 30,
      "path": "res/skill/tabthrough/SKILL.md",
      "ranges": [{ "start": 14, "end": 14 }],
      "significance": "high",
      "title": "The skill tells agents this URL",
      "rationale": "Installed into other repos — this is what made the agent load tabthrough.dev",
      "dependsOn": ["schema-id"]
    }
  ]
}
